// ******************
// Imports

//  Router imports
import { createRouter, createWebHistory } from 'vue-router'

// components that will be loaded as a result of the route


const routes = [
/**
 * example code (for a route)
 * {
 *      path: '/',
 *      name: 'Home',
 *      component: 'HomeView'
 * }
 */
]

export default createRouter({
    history: createWebHistory(),
    routes: routes
})